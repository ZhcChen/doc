---
title: Caddy2 文档
description: Caddy2 文档
---

## 简介
大多数人将 Caddy 用作 Web 服务器或代理，但从核心来看，Caddy 是一个服务器的集合。借助必要的模块，它可以承担任何长时间运行进程的角色！

通过 Caddy 的 API，配置既可以动态调整，也能导出。虽然不需要配置文件，但你仍然可以使用；大多数人最喜欢的配置 Caddy 的方式是使用 Caddyfile。借助配置适配器，配置文件有多种格式，但 Caddy 的原生配置语言是 JSON。

Caddy 可在所有主流平台上编译，并且无需运行时依赖。